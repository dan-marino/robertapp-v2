services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: softball
      POSTGRES_PASSWORD: softball
      POSTGRES_DB: softball
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
